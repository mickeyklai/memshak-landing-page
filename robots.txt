# robots.txt for memshak.co.il
# Allow all search engines to crawl all content

User-agent: *
Allow: /

# Disallow crawling of specific directories (if any in the future)
Disallow: /admin/
Disallow: /.git/
Disallow: /.claude/

# Sitemap (production: Netlify serves dynamic XML — see netlify.toml + netlify/functions/sitemap.js)
Sitemap: https://memshak.co.il/sitemap.xml

# Crawl delay (optional - helps manage server load)
Crawl-delay: 1
